<%= turbo_frame_tag dom_id(@task) do %>
  <%= form_with(model: @task, class: "bg-black py-2 px-2 w-full rounded-lg flex gap-2 items-center justify-between") do |form| %>
    <%= form.text_area :body, rows: 1, placeholder: "Eat a pizza without using hands", class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 w-full text-black" %>
    <%= form.submit class: "rounded-lg py-2 px-5 bg-blue-600 text-white font-medium cursor-pointer" %>
    </div>
  <% end %>
<% end %>
